# Nori Cases

`cases/` stores human-readable project cases. A case is more than raw data: it owns the source brief, brand assets, workflow runs, selected outputs, and showcase files for one client or demo.

Shared runtime caches stay under `data/`, while case-specific source materials and generated artifacts stay under `cases/{case_id}/`.
