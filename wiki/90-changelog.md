<!-- Last verified: 2026-06-01 | Current stage: P1 Account-Ops Backend -->

# Changelog

## 2026-06-08

| Change | Notes |
| --- | --- |
| Expanded delivery review evidence | Case delivery exports now include standalone `artifact_inspection.json` and `review_evidence.json` snapshots so handoff bundles expose proof, acceptance, evaluations, image-reference trace, and visual-reference review without digging through `delivery.json`. |
| Added visual reference review panel | Run detail, artifact inspection, and evaluation-draft context now include `visual_reference_review`, and reference-driven drafts add a `visual_reference` reviewer so sent references remain pending until human visual match review passes. |
| Added reference-image trace evidence | Run detail, manifests, proof, and acceptance evidence now expose per-reference trace rows that connect selected paths, uploaded asset ids, public/provider-fetchable URLs, publish reasons, object keys, and sent status for visual-reference audits. |
| Added backend-public reference publishing | `/sessions/{session_id}/assets/publish-references` can now publish uploaded local assets as provider-fetchable backend file URLs when a real `backend_public_base_url` is supplied, and the Holly smoke script calls that publish route before strict preflight/run while also supporting `--api-base-url` for tunnel-backed tests against a running backend. |
| Added preflight-only reference URL probe smoke | `scripts/backend_holly_smoke.py --verify-reference-urls` now sends `verify_reference_urls=true` during preflight-only strict-reference checks, so broken tunnels/public backend URLs fail before live model calls. |
| Added live reference-image provider check | Added `POST /experiments/content-production/reference-image-generation-check`, an opt-in diagnostic that calls the active image model with provider-fetchable `reference_images` and returns structured success/provider-error evidence. |
| Added session reference-image generation check | Added `POST /sessions/{session_id}/assets/reference-image-generation-check`, which publishes selected uploaded assets and then calls the active image model only when provider-fetchable reference URLs exist. |
| Added URL probe gate to session reference check | The session reference-image generation check now accepts `verify_reference_urls` and stops with `reference_url_probe_failed` before image-model calls when published references are unreachable. |
| Persisted reference-image check evidence | Session reference-image generation checks now append compact `reference_image_generation_checked` session events with readiness, URL probe, provider-fetchable URL, and generation summary evidence. |
| Surfaced latest reference check | Session, session-assets, run-template, and preflight responses now expose the latest reference-image generation check summary so product clients do not need to parse raw session events. |
| Mirrored reference-check evidence into runs | Content-production runs now copy the latest session reference-image check into `input_manifest` and `experiment_manifest`, including whether the checked provider URLs cover the selected references for that run. |
| Added strict provider-reference check gate | Content-production preflight/run requests now support `require_reference_image_generation_check=true`, which rejects generation unless the latest provider check is ready and covers the selected references. |
| Closed provider-check proof and acceptance | Run `proof` and `acceptance` now reject strict experiments that require provider reference-image checks but lack successful selected-reference coverage evidence. |
| Made reference repair actions executable | Case `next-actions` now returns concrete session hrefs and payloads for reference publish/provider-check repairs when run manifests include session and asset ids. |
| Preserved session context in repair reruns | Reference repair replay actions now include the original `session_id`, so provider-check session events survive the repair/rerun loop. |
| Guarded strict replay provider checks | Strict replays now ignore stale provider-check evidence inside `replay_request.json` and require the replay session's current `reference_image_generation_checked` event to cover selected references. |
| Exposed provider reference check actions | Ready strict-reference run templates and preflights now include optional `check_reference_image_generation` actions pointing at the session-asset diagnostic endpoint, so UI flows can validate image-provider reference support before a full run. |
| Wired provider reference check into Holly smoke | `scripts/backend_holly_smoke.py --check-reference-image-generation` now calls the session reference-image generation diagnostic and stops before `--run` when that check fails. |
| Hardened Holly reference check smoke | `scripts/backend_holly_smoke.py --check-reference-image-generation` now returns `no_provider_fetchable_reference_images` and exits before image-model calls when preflight did not expose public reference URLs. |
| Hardened tunnel-backed smoke errors | Running `scripts/backend_holly_smoke.py --api-base-url ...` against an HTTPS tunnel now reports non-JSON API/tunnel responses with route, status, and body preview instead of crashing with a raw JSON decode traceback. |
| Added background smoke polling | `scripts/backend_holly_smoke.py --run --execution-mode background` now follows accepted job links until the workflow reaches a terminal state, so smoke output reports the final run success or failure instead of stopping at HTTP 202. |
| Guarded OpenAI structured-output adapter fallback | `LanguageModelClient.chat_json` now falls back to direct OpenAI-compatible JSON chat only for the OpenAI raw-response parse adapter error, preserving structured-output failures for all other exceptions. |
| Added transient LLM connection retry | Required JSON LLM stages now retry one transient provider connection error before raising the stage-specific failure, protecting long Holly workflow runs from single-call `APIConnectionError` interruptions. |
| Simplified workflow runner boundary | Merged the LangGraph execution engine into `nori.workflows.WorkflowRunner`, removed the separate LangGraph runner module/export, and kept the same stage, human-gate, and artifact-ref behavior. |
| Added run input-integrity proof | Run detail proof now includes `input_integrity`, which recomputes recorded fingerprints from `replay_request.json`, the original brief file, replay config, market evidence, metadata, and local asset hashes; mismatches become blocking proof failures. |
| Added backend run input fingerprints | Content-production `input_manifest.json` now records `execution_mode`, config, and stable SHA256 fingerprints for the replay request, config, market evidence, metadata, brief, and selected asset hashes; `experiment_manifest.inputs` and run comparison mirror these fingerprints for reproducibility audits. |
| Added content-production run template API | Added `GET/POST /experiments/content-production/run-template`, which resolves session metadata or form draft bodies, task goals, uploaded assets, reference policy, and market evidence into a concrete run/preflight request with missing fields and launch actions. |
| Added experiment workbench snapshot | Added `GET /experiments/content-production/workbench`, a one-call console bootstrap response that combines diagnostics, overview, enriched case rows, and backend-derived primary actions to avoid frontend N+1 action assembly. |
| Added case-scoped workbench review data | `GET /experiments/content-production/workbench?case_id=...` now includes `case_compare`, `case_delivery`, `active_run_id`, and `active_run_artifacts`, so case review screens can bootstrap comparison, handoff readiness, and artifact panels in one backend call. |
| Linked case review writes from workbench | Workbench top-level links and case-row links now expose `case_evaluation_draft` and `case_evaluations`, letting frontend review buttons call case-level evaluation APIs without hard-coded URL assembly. |
| Expanded case index links | Case summaries returned by overview, cases, workbench, and compare now expose backend-owned case-level review, replay, delivery, timeline, and export links so lightweight case selectors can drive actions without URL reconstruction. |
| Routed first-run actions through templates | `needs_first_run` now points `run_first_experiment` to the backend run-template endpoint, so launch UI resolves missing fields and preflight-ready payloads before calling the live run API. |
| Standardized run-template actions | Run-template actions now consistently expose `method`, `href`, optional `payload`, and `input_fields` for form-repair actions, letting frontend launch forms render backend-derived next steps directly. |
| Added preflight action contracts | Content-production preflight responses now include backend-derived `actions` and `links`; ready preflights expose `run_experiment`, and failed preflights or run-gate HTTP 400 responses expose repair actions for market evidence and reference transfer. |
| Added post-run action contracts | Sync run results, failed run payloads, background jobs, and restored job snapshots now expose backend-owned run/case `links` plus ordered `actions` for artifact inspection, evaluation draft, case next actions, replay, export, and promotion. |
| Added background job cancellation API | Added `POST /experiments/jobs/{job_id}/cancel`; queued jobs can become `cancelled`, running in-process jobs become `cancelling` with `cancel_requested=true` and stay pollable until the worker returns. |
| Synced cancelled jobs back to sessions | Cancelling a session-backed background experiment now updates the linked task to `cancelled` or `cancelling`, returns a session sync summary, and writes `workflow_run_cancelled` / `workflow_run_cancel_requested` events. |
| Synced interrupted job restore back to sessions | On backend startup, restored queued/running/cancelling job snapshots are persisted as `interrupted`; linked session tasks are updated to `interrupted` and receive a `workflow_run_interrupted` event. |
| Added workflow stage timeout evidence | Runtime `StageSpec` now supports `timeout_seconds`; content-production stages record timeout metadata, failed runs persist `workflow_run.json`/`experiment_manifest.json`, and `scripts/backend_holly_smoke.py --run` can override stage and content-package timeouts for live backend debugging. |
| Routed JSON calls through LangChain structured output | `nori.core.llms.chat_json` now uses `with_structured_output(..., include_raw=True)` as the primary path and keeps a targeted plain-chat JSON fallback for known structured-output adapter parse failures. |
| Kept chat gateway on LangChain adapters | `nori.core.llms.chat/achat` continue to use the configured LangChain chat adapter; structured JSON calls now have a targeted plain-chat fallback for the SDK `.parse` incompatibility seen in live content-production runs. |
| Guarded strict-reference public URLs | Backend preflight, run manifests, and `scripts/backend_holly_smoke.py --run --require-image-references` now share a provider-fetchable URL rule that rejects placeholder/local hosts such as `backend.example.test`, `localhost`, private IPs, and RFC example domains before strict reference runs. |
| Added strict-reference URL reachability probe | Content-production run/preflight requests now support `verify_reference_urls` and `reference_url_probe_timeout`; when enabled, the backend probes selected reference URLs and adds a `reference_url_reachability` check plus `reference_images.url_probe` details before live model calls. |
| Added case comparison snapshot | Added `GET /experiments/content-production/cases/{case_id}/compare`, a case-centered decision payload that combines selection state, best/recommended run ids, candidate rows, run differences, and next actions for experiment-console review pages. |
| Added case next-action planner | Added `GET /experiments/content-production/cases/{case_id}/next-actions`, which turns report, selection, acceptance, evaluation, and reference-transfer state into ordered product actions such as select best run, draft evaluation, fix references, replay, or promote. |
| Added case promotion action | Added `POST /experiments/content-production/cases/{case_id}/promotion`, which verifies an accepted run, records a `promoted` selection, returns artifact/export/replay links, and requires explicit override for non-accepted promotions. |
| Added case-level replay action | Added `POST /experiments/content-production/cases/{case_id}/replay`, which resolves an explicit, selected, or best run and reuses the stored `replay_request.json` without requiring clients to construct run-level replay URLs. |
| Added case-level evaluation actions | Added `POST /experiments/content-production/cases/{case_id}/evaluations/draft` and `/evaluations`, which resolve explicit, selected, or best runs and write through the existing run evaluation store so case review screens no longer assemble run-level review URLs. |
| Added case delivery gate | Added `GET /experiments/content-production/cases/{case_id}/delivery`, which returns formal handoff readiness, blockers, warnings, accepted-run proof, artifact inspection, compare data, next actions, and export/artifact links. |
| Added delivery-gated handoff exports | Added `GET /experiments/content-production/cases/{case_id}/delivery/export`, which refuses non-ready cases by default and streams a handoff zip with delivery metadata, compare/action snapshots, run summary, and whitelisted run artifacts. |
| Added selected-run resolver | Added `GET /experiments/content-production/cases/{case_id}/selected-run`, which returns full run detail for the current operator selection or, by default, falls back to the case report's `best_run` without mutating selection state. |
| Added case-level experiment timelines | Added `GET /experiments/content-production/cases/{case_id}/timeline`, a read-only audit view that orders run start/finish, evaluation, and selection events for experiment-console history and debugging. |
| Added case-level experiment export bundles | Added `GET /experiments/content-production/cases/{case_id}/export`, which downloads a zip containing the case report, selection history, case summary, per-run summaries, and an export manifest for experiment handoff and archival. |
| Surfaced experiment selection in case summaries | Content-production overview and cases rows now include the current `selection`, `selected_run_id`, `selection_decision`, and selection route link so experiment consoles can show decision state without per-case follow-up requests. |
| Added case-level experiment selection | Added `GET/POST /experiments/content-production/cases/{case_id}/selection`, which persists the current operator-selected run, append-only decision history, reviewer/reason/metadata, and whether the selected run matches the report's recommended best run. |
| Added content-production experiment reports | Added `GET /experiments/content-production/report`, a case-level report that selects the strongest run, separates accepted/needs-review/rejected ids, summarizes blockers/warnings/evaluation issues, and recommends next actions for the experiment console. |
| Added main-chat supervisor agent | Added `nori.agents.supervisor.NoriSupervisorAgent`, supervisor schemas, default subworkflow/subagent tool catalog, LLM/keyword routing, plan-only mode, injected tool execution, and clarification fallback. |
| Moved architecture visuals into wiki | Moved static architecture HTML/SVG artifacts to `wiki/visuals/` so root-level directories stay limited to product/runtime boundaries. |

## 2026-06-07

| Change | Notes |
| --- | --- |
| Promoted LLM gateway into core infra | Moved language/vision/image gateway implementation under `nori.core.llms`, stopped packaging `llms*`, and added LangChain Core message normalization for chat inputs. |
| Removed top-level LLM compatibility package | Migrated in-repo tests and monkeypatch paths to `nori.core.llms`, deleted the old top-level `llms/` source-tree alias, and added architecture coverage that prevents reintroducing `import llms`. |
| Cleaned LLM gateway internals | Reused LangChain Core JSON markdown/partial parsing and message conversion, moved LLM execution into class-backed clients, and removed unconnected structured prompt helpers. |
| Routed chat through LangChain init API | Text/vision `chat` / `achat` now use chat adapters created via `init_chat_model`, while keeping Nori config validation, request-param merging, capability guards, telemetry, and image SDK provider paths. |
| Added Guizang social-card design profile | Distilled `guizang-social-card-skill` into `nori.agents.content_generation.social_card_guides` instead of vendoring external templates. |
| Enriched content specs for social cards | XHS image-text specs now carry 1080x1440 canvas rules, safe area, 5-9 page guidance, page roles, compression ladder, style-mode identity checks, and density/thumbnail/evidence QA checks. WeChat article specs now carry 21:9 + 1:1 cover-pair guidance. |
| Propagated spec constraints into cover prompts | `CoverPromptBuilder` reads `intent.content_design_spec` so cover generation sees the social-card platform/profile constraints selected by `ContentSpecAgent`. |
| Split frontend boundaries | Added `web/` for product frontend/prototypes, moved the static Nori demo under `web/prototypes/`, separated architecture visualizations from product UI, and removed the empty `nori/api` placeholder before adding the concrete top-level `backend/` service boundary. |
| Added lightweight backend boundary | Added `backend/` as a FastAPI service with `{code, message, data}` response helpers, session routes, workflow catalog routes, Swagger/OpenAPI docs, and a uvicorn entrypoint. The backend calls stable Nori runtime contracts and does not import agent implementation modules. |
| Added content-generation product API catalog | Added `/capabilities`, `/content/generation/options`, `/content/generation/actions`, `/content/generation/plan`, and `/workflows/resolve` so product surfaces can call structured sub-capabilities or resolve to a workflow without always passing a workflow id. |
| Added backend experiment execution path | Added multipart session image uploads, session asset listing, and `POST /workflows/content-production/runs`, which resolves uploaded image assets into workflow `asset_paths` and runs the real content-production workflow with caller-supplied market evidence. |
| Added strict image-reference guard | `require_image_references=true` now makes backend/workflow cover generation fail explicitly when selected local references cannot be sent as provider-supported `reference_images`, instead of silently falling back to text-only image generation. |
| Added backend experiment observability | Added `/experiments/readiness`, content-production run listing/detail APIs, and `image_reference` summaries so product surfaces can see model/storage readiness, stage status, artifact paths, cover outputs, and whether selected reference images were actually sent. |
| Added backend artifact file serving | Added safe backend file endpoints for uploaded session assets and whitelisted content-production run artifacts/covers, plus `artifact_urls`, `cover_urls`, and asset `file_url` fields for product clients. |
| Added backend asset reference publishing | Added `POST /sessions/{session_id}/assets/publish-references`, which publishes selected uploaded images through `nori.storage.ReferenceImagePublisher`, stores `public_reference_url` on session asset rows, and reports `ready=false` instead of fabricating URLs when publishing is unavailable. |
| Added backend public-reference URL bridge | Backend content-production runs can now map uploaded local assets to public backend file URLs via `NORI_BACKEND_PUBLIC_BASE_URL` or request-level `backend_public_base_url`, letting CoverDirector send relay-compatible HTTPS `reference_images` without requiring OSS. |
| Added local session snapshots | `NoriBackend` now uses a durable `SessionManager` by default, writing local JSON snapshots under `data/backend/sessions` so experiment sessions, task status, uploaded asset metadata, and published reference URLs survive backend restarts. |
| Added backend run preflight | Added `POST /workflows/content-production/runs/preflight`, which validates session/task inputs, selected assets, market evidence, model readiness, and provider-fetchable reference URLs without creating a task or calling LLM/image providers. |
| Hardened strict reference runs | `require_image_references=true` now rejects runs with no selected image assets before task creation, preventing strict-reference experiments from succeeding without any image to reference. |
| Added backend input manifests for experiments | Content-production runs now write and expose `input_manifest.json`, including session/task ids, brief hash, selected assets, public reference URL mapping, strict-reference mode, human-gate mode, and market-evidence counts for replay and audit. |
| Added backend replay request snapshots | Content-production runs now persist `replay_request.json` with the original brief, config, full market evidence, run options, and selected asset paths so experiments can be reconstructed without scraping API logs. |
| Added backend replay execution API | Added `POST /workflows/content-production/runs/{case_id}/{run_id}/replay`, which loads a stored `replay_request.json`, creates a fresh replay session/task by default, validates saved asset paths, supports safe overrides, and can run sync or through the existing background job API. |
| Added background experiment jobs | `POST /workflows/content-production/runs` now accepts `execution_mode="background"` and returns HTTP 202 with a pollable in-process job at `/experiments/jobs/{job_id}`, including success results or structured failure errors. |
| Persisted local job snapshots | Background experiment jobs are now written to `data/backend/jobs`, can be listed through `GET /experiments/jobs`, and can be filtered by status/session/case/job type for local audit and debugging. |
| Hardened experiment readiness diagnostics | `/experiments/readiness` now returns top-level `ready=false` plus per-model `error_type`/`error` fields when active model lookup fails, instead of turning configuration problems into a 500 response. |
| Added backend experiment manifests | Content-production runs now write and expose `experiment_manifest.json`, grouping run identity, session/task ids, selected inputs, model snapshot, reference-image fidelity, artifact URLs, replay endpoint, and structured failure metadata. |
| Added backend run comparison | Added `GET /workflows/content-production/runs/compare` so product clients can compare recorded runs by input differences, artifact coverage, image-reference fidelity, model snapshot, and per-run review-readiness blockers. |
| Added backend run evaluations | Added `GET/POST /workflows/content-production/runs/{case_id}/{run_id}/evaluations`; evaluations persist as `experiment_evaluations.json`, refresh `experiment_manifest`, and feed run-detail/compare readiness blockers. |
| Added Holly backend API smoke | Added `backend.fixtures.holly_content_production_fixture` and `scripts/backend_holly_smoke.py`, a preflight-only default smoke that creates a backend session, uploads Holly fixture images through FastAPI, and can optionally execute the real workflow with `--run`. |
| Added content-production diagnostics | Added `GET /experiments/content-production/diagnostics`, a setup-screen diagnostic API with model/reference/OSS/backend-public-URL checks, blocking/warning check lists, and recommended actions. |
| Added reference publish check | Added `POST /experiments/content-production/reference-publish-check`, which verifies the configured reference publisher using a backend-owned tiny PNG and returns public URL/object-key readiness details. |
| Added content-production experiment overview | Added `GET /experiments/content-production/overview`, a dashboard-level aggregate of recorded run status, reference-image fidelity, evaluation status, review readiness, per-case summaries, and blocker counts. |
| Added content-production cases API | Added `GET /experiments/content-production/cases`, a lightweight case index for experiment sidebars and selectors with latest run/export/replay links and readiness counts. |
| Added run list filtering | `GET /workflows/content-production/runs` now supports case/status/proof/reference/evaluation filters, search, pagination, filtered counts, and status summaries for experiment-console tables. |
| Hardened backend run gates | Real content-production runs now reject unready model configuration, missing `market_evidence`, and strict-reference transfer failures before creating a task or calling the workflow, reusing the same deterministic checks surfaced by preflight. |
| Recorded reference-transfer snapshots | Content-production input and experiment manifests now include `reference_transfer`, a pre-generation audit record of selected images, provider-fetchable reference URLs, and strict public-URL readiness. |
| Added run proof summaries | Content-production run detail now returns a derived `proof` object with named checks, failed/warning check lists, reference evidence, artifact links, and evaluation state for experiment-detail UIs. Overview latest-run rows expose proof status summaries. |
| Added run artifact catalog | Content-production run detail now returns `artifact_catalog`, a whitelisted artifact panel with URLs, media types, sizes, SHA-256 hashes, and short JSON/Markdown previews. |
| Added run artifact inspection | Added `GET /workflows/content-production/runs/{case_id}/{run_id}/artifacts/inspect`, which groups whitelisted artifacts into product-ready content package, manifest, Markdown, cover, proof, acceptance, and evaluation sections for run review panels. |
| Added backend run export bundles | Added `GET /workflows/content-production/runs/{case_id}/{run_id}/export`, which streams a zip bundle with whitelisted run artifacts, cover images, and a generated export manifest for review and experiment archiving. |
| Added run acceptance reports | Content-production run detail and `GET /workflows/content-production/runs/{case_id}/{run_id}/acceptance` now expose a derived accepted/needs_review/rejected verdict with blocking/warning checks and evidence for artifacts, replay/export, strict references, and evaluations. |
| Added automatic evaluation drafts | Added `POST /workflows/content-production/runs/{case_id}/{run_id}/evaluations/draft`, which restores run artifacts/context, calls the existing `ReviewGateAgent`, returns reviewer issues and an evaluation draft, and can persist it with `persist=true`. |
| Added optional input assets to export bundles | Run export bundles now accept `include_inputs=true` to embed local image inputs from `input_manifest.assets` under `inputs/`, while reporting skipped remote/missing/unsupported inputs in the export manifest. |
| Linked background jobs to run surfaces | Successful content-production job records now expose direct run, export, evaluation, and replay links, including after job snapshots are restored from disk. |
| Preserved failed experiment run data | Failed content-production workflows now surface the failed run summary in sync API error `data.run` and background job `result`, preserving artifact links, manifests, and replay/export paths for failure review. |
| Moved newer web demo | Moved the local `New project 2/` demo files into `web/prototypes/nori-workbench-v2-demo/` without carrying over its empty nested `.git/` directory. |

## 2026-06-01

| Change | Notes |
| --- | --- |
| Repaired runtime entrypoints | `main.py` now reports supported CLI/workflow entrypoints and can dispatch the Holly live workflow instead of importing the missing `nori.nori.server`. |
| Repaired live smoke imports | `scripts/smoke_note_maker.py` and `scripts/smoke_session_skill.py` now bootstrap the repo root and import canonical `nori.agents.*` / `nori.core` modules. |
| Added runtime entrypoint guards | Added subprocess tests for `main.py` and smoke-script `--help`, plus a stale-root scan covering removed legacy imports. |
| Renamed planning tests | Renamed `tests/test_context_building_*` files to current `planning_*`, `user_profiling_*`, and `market_analysis_*` names and added a guard against reintroducing the stale prefix. |
| Split core model ownership | Replaced the large `nori.core.models` implementation with focused owner modules: `profile_models`, `asset_models`, `planning_models`, and `capability_models`; the temporary `nori.core.models` facade was later removed. |
| Moved module inventory to reference docs | Reduced `wiki/20-system-architecture.md` to runtime-layer decisions and moved detailed package/helper maps to `wiki/refs/module-map.md`. |
| Updated verification baseline | `python -m pytest tests -q` reports `503 passed, 3 skipped`. |

## 2026-05-26

| Change | Notes |
| --- | --- |
| Removed legacy contract compatibility modules | Deleted `nori.config_models`, `nori._model_coercion`, `nori.agents.planning.models`, `nori.core.llms.errors`, and `nori.core.llms.structured_models`; tests now assert these old import roots are absent. |
| Promoted LLM public contracts to core | Added `nori.core.contracts` as the owner for runtime config dataclasses, gateway errors, structured LLM result dataclasses, and shared model coercion helpers. |
| Made core exports lazy | `nori.core` now lazy-loads public contracts, domain models, runtime bases, workflow helpers, and architecture metadata, reducing import side effects while keeping existing public imports stable. |
| Guarded private contract imports | Added architecture coverage so runtime code imports public contracts from `nori.core` / `nori.core.contracts` instead of private compatibility modules. |
| Added core runtime bases | Added `nori.core.LLMFactory`, `AgentBase`, and `WorkflowBase`; concrete runtime agents now inherit `AgentBase`, and orchestration agents can share the same injectable LLM gateway. |
| Standardized domain facades as workflows | `UserProfilingFacade`, `MarketAnalysisFacade`, `ContextPackBuilder`, `ContentGenerationFacade`, and `LearningLoopFacade` now inherit `WorkflowBase` and expose stable `workflow_name` / `step_names` values. |
| Enforced core LLM injection boundary | Removed direct `llms` imports from workflow runtime stages; tests now patch the project gateway or inject factories, and architecture guards require stage code to route LLM access through `nori.core.LLMFactory`. |
| Promoted shared workflow contracts to core | Moved `AssetRecord`, `AssetLibrary`, `ClientBrief`, `OperationPlan`, `KPIPlan`, `ContentTask`, and `ContentCalendar` to focused `nori.core.*_models` owners; runtime code imports them from `nori.core`. |
| Promoted project aggregate to core | Moved `AccountOperationProject` to `nori.core.project`; planning stages import it from core and no longer carry a model re-export layer. |
| Standardized stage package contracts | Stage-local `inputs.py`, `prompts.py`, and `refs.py` support files are now consolidated into class-owned `package.py` contracts where they only serve one agent; model contracts stay in canonical owner-local `schemas/` packages or `nori.core`. |
| Updated verification baseline | `python -m pytest tests -q` now reports `448 passed, 3 skipped` after adding core runtime abstraction tests, stage-support-module guards, the no-direct-`llms` runtime guard, shared workflow-contract ownership guards, project-aggregate ownership guards, and domain-facade workflow guards. |
| Removed legacy agent/model roots | Deleted `nori/gen_agents`, `nori/ops_agents`, `nori/ana_agents`, `nori/ops_models`, `nori/agent_models`, and `nori/agent_utils`; tests now assert those import roots are absent. |
| Removed canonical-package flat aliases | Removed flat helper aliases from workflow package roots and updated tests/docs to import each helper from its owning stage folder. |
| Tightened stage package roots | Replaced `import *` package re-exports with explicit public exports and moved monkeypatch tests to real entry modules. |
| Trimmed XHS analyzer orchestration file | Removed private pass-through helpers from the main analyzer file so rule, loader, LLM, clustering, reporting, and skill-building logic stay in their owning modules. |
| Trimmed agent entry orchestration wrappers | Removed no-value private pass-through helpers from generation, profiling, planning, review, strategy, and analyzer entry modules so orchestration calls owning helper modules directly. |
| Moved user-profiling model ownership | Moved `UserInput`, `IntakeResult`, `AccountPlannerInput`, and `AccountPlanResult` to `nori.agents.user_profiling.schemas`; added an architecture guard against imports from the removed old owner path. |
| Moved content-generation model ownership | Moved `AssetBundle`, `CandidateTitle`, `NoteDraft`, and `CoverResult` to `nori.agents.content_generation.schemas`; added an architecture guard against imports from the removed old owner path. |
| Promoted UserAsset to shared core contract | Moved `UserAsset` to `nori.core.asset_models` because it is produced by Intake and consumed by generation; `nori.agents.content_generation.schemas` re-exports the same class for existing generation call sites. |
| Moved market-analysis model ownership | Moved `XHSNoteSample`, `XHSSeedSkillDraft`, `NoteEvidence`, `NoteSkill`, and `SessionSkillReport` to `nori.agents.market_analysis.schemas`; added an architecture guard against imports from the removed old owner path. |
| Removed agent model compatibility root | Deleted `nori/agent_models` and renamed model contract tests to `tests/test_domain_model_contracts.py`. |
| Moved shared runtime utilities | Moved case logging, image IO, and JSON LLM helpers to `nori/shared`; deleted `nori/agent_utils` and renamed utility tests to `tests/test_shared_utils.py`. |
| Moved NoteSkill fixture helper to market analysis | Moved `load_note_skills`, `note_skill_fixture`, and `write_note_skill_fixture` from `nori.shared` to `nori.agents.market_analysis.note_skill_fixture`; added a guard that `nori/shared` must not import business modules. |
| Renamed shared runtime helper surface | Replaced historical `call_agent_*`, `try_agent_*`, and `write_agent_log` names with stage-oriented `call_stage_*`, `try_stage_*`, and `write_stage_log`; renamed the architecture guard to `tests/test_workflow_folder_architecture.py`. |
| Centralized package-root lazy exports | Added `nori.core.lazy_exports.lazy_export` and routed five business package roots through one shared helper for facade/stage exports, preserving public imports without reintroducing domain-module cycles. |
| Renamed trace stage history field | `ExplanationTrace` now writes `stage_steps` and normalizes legacy `agent_steps` / row-level `agent` keys to `stage` during `from_dict()`. |
| Decoupled upstream facades from project aggregate internals | `UserProfilingFacade` and `MarketAnalysisFacade` now consume project-like dict/object shapes instead of importing `AccountOperationProject`; added dependency-direction guards. |
| Updated legacy-cleanup verification baseline | `python -m pytest tests -q` reported `434 passed, 3 skipped` after deleting legacy compatibility tests and adding export-boundary/lazy-export/trace-field/shared-boundary/dependency-direction/core-asset guards. |

## 2026-05-24

| Change | Notes |
| --- | --- |
| Migrated ops/analysis agent implementations | Moved concrete implementations into the five workflow modules. Superseded on 2026-05-26 by removal of the legacy roots. |
| Grouped stage files by owning stage package | Moved each concrete runtime stage into a folder with a specifically named entry module plus local helper modules and added `tests/test_workflow_folder_architecture.py` to prevent regression. |
| Initialized canonical wiki | Added product proposal, roadmap, glossary, architecture, design principles, API reference, conventions, stage docs, pitfalls, backlog, and changelog. |
| Captured current architecture | Documented LLM gateway, generation chain, account-ops backend, data collection, and live dependency boundaries. |
| Added wiki maintenance rules | Created `CLAUDE.md` with wiki synchronization guidance. |
| Restored default test baseline | Added Python 3.9 dataclass compatibility, restored Holly showcase fixture, and verified `python -m pytest tests -q` now passes. |
| Updated README canonical entry | README now points to `wiki/` as the source of truth and treats historical notes under `wiki/archive/legacy-docs/` as reference material. |
| Added P3 production bridge | Implemented `ContentProducerAgent` / `produce_content_package` for `ContentTask -> ContentPackage`, with prompt snapshots, material usage, source refs, and structured failure metadata. |
| Consolidated content package assembly | Replaced tiny `inputs` / `builder` / `refs` helper modules with class-based `ContentPackageAssembler` in `nori.agents.content_generation.content_producer.package`, inheriting shared stable artifact slug/dedupe behavior. |
| Extracted content production state boundary | Moved production error formatting, stage classification, and task/project success/failure metadata updates into `nori.agents.content_generation.content_producer.state`, with focused state tests. |
| Added P4 review gate | Implemented rule-based compliance and consistency reviewers returning `ComplianceReview`, with project attachment and offline tests. |
| Consolidated review package contract | Moved package/task/client-brief restoration and project-derived brief fallback into class-owned `nori.agents.learning_loop.review.package`, with focused package tests. |
| Extracted review policy boundary | Moved compliance/consistency issue calculation, score/status mapping, severity metadata, and fix suggestions into `nori.agents.learning_loop.review.policy`, with focused policy tests. |
| Extracted review scoring boundary | Moved issue-row construction, severity penalties, score/status mapping, severity counts, and deduped fix suggestions into `nori.agents.learning_loop.review.scoring`, with focused scoring tests. |
| Extracted review state boundary | Moved optional project compliance-review attachment into `nori.agents.learning_loop.review.state`, with focused state tests. |
| Consolidated XHS analyzer package contract | Moved note enhancement, keyword generation, and label prompt text into class-owned `nori.agents.market_analysis.xhs_note_analyzer.package`; `note_llm` and `session_llm` now use prompt builders instead of a shared `prompts.py` constants module. |
| Added P4 iteration loop | Implemented manual `MetricsSnapshot` recording and rule-based `StrategyIteration` creation from reviews + metrics. |
| Consolidated strategy iteration package contract | Moved review/metrics snapshot restoration and project-derived evidence fallback into class-owned `nori.agents.learning_loop.strategy.package`, with focused package tests. |
| Extracted strategy iteration policy boundary | Moved metric alias normalization, review/metric summaries, diagnosis, decisions, and next-action rules into `nori.agents.learning_loop.strategy.policy`, with focused policy tests. |
| Extracted strategy iteration state boundary | Moved metrics snapshot and strategy iteration project attachment into `nori.agents.learning_loop.strategy.state`, with focused state tests. |
| Consolidated OperationPlanner package contract | Moved `ClientBrief` / `AccountPlanResult` restoration, run-window normalization, and JSON-only SOP prompt construction into class-owned `nori.agents.planning.operation_planner.package`, with focused package tests. |
| Extracted OperationPlanner fallback builder boundary | Moved deterministic project fallback construction, rule-based tasks, calendar/KPI derivation, asset requirements, and benchmark references into `nori.agents.planning.operation_planner.project_builder`, with focused module tests. |
| Extracted OperationPlanner fallback policy boundary | Moved content-pillar/objective/topic/risk/reference derivation, default milestones, project labels, shared calendar policy reuse, and operation-derived KPI policy into `nori.agents.planning.operation_planner.project_policy`, with focused policy tests. |
| Extracted OperationPlanner LLM normalizer boundary | Moved operation-plan/calendar JSON merging, task-row normalization, date clamping, and derived KPI snapshot creation into `nori.agents.planning.operation_planner.normalizer`, with focused module tests. |
| Consolidated KPIPlanner package contract | Moved operation/project/composite-dict restoration, project-context derivation, and JSON-only KPI prompt construction into class-owned `nori.agents.planning.kpi_planner.package`, with focused package tests. |
| Extracted KPIPlanner normalizer boundary | Moved KPI fallback defaults, LLM KPI merge, milestone clamping, and empty measurement-note fallback preservation into `nori.agents.planning.kpi_planner.normalizer`, with focused module tests. |
| Consolidated CalendarPlanner package contract | Moved operation/project/composite-dict restoration, KPI/brief override normalization, run-window selection, and JSON-only calendar prompt construction into class-owned `nori.agents.planning.calendar_planner.package`, with focused package tests. |
| Extracted CalendarPlanner normalizer boundary | Moved calendar fallback construction, LLM task-row merge, scheduled-day clamping, empty-list fallback preservation, and task-count caps into `nori.agents.planning.calendar_planner.normalizer`, with focused module tests. |
| Extracted CalendarPlanner policy boundary | Moved bounded horizon/start-date rules, task-count limits, scheduled-date/day clamping, cadence/topic labels, and required asset fallback into `nori.agents.planning.calendar_planner.policy`, with focused policy tests. |
| Extracted CalendarPlanner task builder boundary | Moved fallback `ContentTask` row construction and LLM task-row cleanup into `nori.agents.planning.calendar_planner.task_builder`, with focused task-builder tests. |
| Extracted shared planner critic policy | Moved Operation/KPI/Calendar structural completeness checks, fallback warnings, task readiness checks, and KPI/calendar alignment into `nori.agents.planning.planner_critics`, with focused policy tests. |
| Added redacted LLM config contract | Added `api_config.example.yaml`, `wiki/refs/api-config.md`, `api_key_env` support, `NORI_CONFIG` fail-fast behavior, and config tests. |
| Added P1 evidence models | Implemented `AssetLibrary` and `CompetitorResearch` models with project nesting and serialization tests. |
| Added typed account positioning | Replaced loose project positioning dict with `AccountPositioning`, preserving legacy dict round trips and planner context compatibility. |
| Added NoteSkill fixture bridge | Added `NoteSkill` / `SessionSkillReport` deserialization plus `load_note_skills` and `write_note_skill_fixture` helpers for learned-skill reuse. |
| Added redacted data-collection runbook | Documented CookieBridge/sign/downloader health checks, live top-notes smoke, session-skill smoke, and stop conditions without secrets. |
| Extracted XHS note loader boundary | Moved local note/author `meta.json` restoration, tag extraction, and platform metric count parsing into `nori.agents.market_analysis.xhs_note_loader`, with focused loader tests. |
| Extracted XHS single-note rule boundary | Moved rule-only seed draft construction, scene/goal classification, content rule extraction, CTA evidence, and confidence scoring into `nori.agents.market_analysis.xhs_note_rules`, with focused rule tests. |
| Extracted XHS single-note LLM enhancement boundary | Moved optional single-note prompt text, JSON routing, model-output normalization, fallback draft marking, and `validation.llm_error` attachment into `nori.agents.market_analysis.xhs_note_llm`, with focused optional-stage tests. |
| Extracted XHS session clustering boundary | Moved rule-goal classification, required LLM-label validation, top-four bucket selection, tone majority, and leftover note tracking into `nori.agents.market_analysis.xhs_session_clustering`, with focused clustering tests. |
| Extracted XHS session LLM boundary | Moved keyword generation, hot-note prompt shaping, goal/tone label normalization, and fail-fast JSON helper routing into `nori.agents.market_analysis.xhs_session_llm`, with focused session LLM tests. |
| Extracted XHS session reporter boundary | Moved report timestamping, full session report writing, and skills-only guide JSON writing into `nori.agents.market_analysis.xhs_session_reporter`, with focused reporter tests. |
| Extracted XHS session skill-builder boundary | Moved session `NoteSkill` construction, merged rules, evidence-note mapping, cover rules, metric percentiles, note-type majority, and cluster signals into `nori.agents.market_analysis.xhs_skill_builder`, with focused builder tests. |
| Extracted LLM gateway errors | Added stable gateway exception classes. Current ownership is `nori.core.contracts`, with public re-exports from `nori.core.llms`. |
| Extracted LLM telemetry boundary | Moved redacted telemetry state and emit logic into `nori.core.llms.telemetry`, keeping public `set_telemetry_sink` imports compatible. |
| Moved LLM chat execution into client class | Sync/async chat adapter resolution, kwargs merge, capability guard, LangChain message content extraction, and chat telemetry now live in `LanguageModelClient`. |
| Routed LLM JSON through client structured output | JSON object calls now live in `LanguageModelClient` through LangChain `with_structured_output`; package-root parser exports were removed. |
| Removed LLM raw JSON fallback | JSON-mode raw chat calls, response-format fallback, and retry classification were removed from `LanguageModelClient`. |
| Moved request-param policy into client classes | Chat kwargs merging and token-limit normalization live in `LanguageModelClient`; image kwargs merging lives in `ImageClient`. |
| Extracted LLM capability boundary | Moved chat/vision/image-reference capability guards into `nori.core.llms.capabilities`. |
| Moved provider output normalization into client/provider classes | Chat text normalization lives privately in `LanguageModelClient`; image URL/data-uri normalization lives with `ImageProviders`. |
| Removed legacy OpenAI chat response parsing | Text chat now consumes LangChain message `content` only; old SDK-shaped compatibility parsing was removed. |
| Extracted LLM image-input boundary | Moved reference-image bytes/data-uri/path/base64 normalization and MIME/data-uri helpers into `nori.core.llms.image_inputs`. |
| Extracted LLM image-provider boundary | Moved OpenAI-compatible edit, relay reference-payload retry, and Google native image generation into `ImageProviders`. |
| Moved LLM image execution into client class | Active image-model resolution, reference input filtering, image capability guards, provider dispatch, result validation, and image telemetry now live in `ImageClient`. |
| Folded LLM runtime config/mode bridges into client core | `ProviderConfig`, `ModelConfig`, and `ResolvedModel` are owned by `nori.core.contracts`; `nori.core.llms.client` now owns the LLM config singleton, active-model access, direct/ghc mode switching, readiness checks, and client factories. The intermediate `config.py` and `mode.py` splits were removed. |
| Extracted runtime config normalization boundary | Moved provider/model key parsing, env-name cleanup, mode normalization, section-shape validation, and active-model selection into `nori.config_normalization`, shared by `nori_config` and `nori.core.llms.client`. |
| Removed unused structured LLM helper modules | Deleted the unconnected intent/target helper prompts, normalization module, structured result models, and non-throwing structured call wrapper from `nori.core.llms`; product-specific structured helpers now belong in the owning agent/supervisor package. |
| Moved structured JSON-call handling into shared helper/client | Stage error translation stays in `nori.shared.llm_json`; provider JSON execution is owned by `LanguageModelClient` through LangChain structured output. |
| Kept JSON fallback reason local to shared helper | `try_stage_json` now classifies empty vs malformed JSON inside `nori.shared.llm_json`, without depending on a core LLM structured-output module. |
| Hardened LLM helper JSON contracts | `chat_json` now uses LangChain structured output and offline tests mock `llms.chat_json` directly instead of relying on raw `llms.chat` injection. |
| Archived root progress tracker | Moved `进度.md` to `wiki/archive/进度.md` and updated project-operator instructions to write current status into wiki/backlog. |
| Added chat result error contract | `nori.core.llms.chat` / `achat` now raise `ChatResultError` and emit failure telemetry when provider responses contain no usable text content. |
| Normalized malformed chat results | Empty or missing LangChain message `content` now raises `ChatResultError` for sync and async chat calls instead of leaking empty success. |
| Guarded image reference capability | Added `ImageCapabilityError` and tests so unsupported reference-image requests fail before provider SDK dispatch. |
| Added image result error contract | `nori.core.llms.image` now raises `ImageResultError` and emits failure telemetry when provider responses contain no usable URL/base64 image. |
| Added redacted LLM telemetry hook | Added `set_telemetry_sink` with prompt-free model call metadata for chat, async chat, and image calls. |
| Made LLM kwargs merge side-effect-free | Chat/image gateway calls now copy caller `extra_body` before applying model-level fields, preventing hidden mutation of request kwargs. |
| Merged image model extra_body safely | OpenAI-compatible `nori.core.llms.image` requests now apply model-level `extra_body` through an image-specific kwargs path without mutating caller kwargs or adding chat-only token/temperature fields. |
| Normalized token limit kwargs | Chat gateway kwargs now emit only the active model's expected token-limit parameter: `max_completion_tokens` for GPT-5 and `max_tokens` for other chat models, independent of whether a model-level `max_output` default exists. |
| Removed embedded JSON parsing from gateway | `chat_json` no longer scans model text for fenced or embedded JSON; LangChain structured output is the JSON parsing boundary. |
| Removed JSON-mode retry classification | `chat_json` no longer owns response-format compatibility classifiers because structured output binding is delegated to LangChain. |
| Removed JSON-mode TypeError retry classification | The TypeError retry path was removed with the raw-chat JSON fallback. |
| Aligned model capability metadata | Preserved video/audio fields on `ResolvedModel` and added active image model type validation. |
| Guarded chat/vision capability | Added `ChatCapabilityError` and tests so non-chat models, vision usage on non-vision models, and multimodal messages on non-vision models fail before provider SDK dispatch. |
| Removed public JSON raw-capture helper | `chat_json` owns retry and parse behavior; raw-capture plumbing is private to `LanguageModelClient`, and package root no longer exposes `chat_json_with_raw`. |
| Shared generation JSON wrapper | Added `nori.shared.call_stage_json` and routed NoteMaker/CoverDirector JSON stages through it with domain-specific error translation. |
| Shared pre-built messages JSON wrapper | Added `nori.shared.call_stage_messages_json` and routed Intake vision tagging through it so multimodal JSON stages also request JSON mode consistently. |
| Shared optional pre-built messages JSON wrapper | Added `nori.shared.try_stage_messages_json` and made `try_stage_json` delegate to it so optional custom/multimodal JSON stages share one fallback metadata contract. |
| Extracted Intake text-normalizer boundary | Moved deterministic text fallback, optional text-LLM output cleanup, alias mapping, image context construction, and missing/question fallback into `nori.agents.user_profiling.intaker.normalizer`, with focused module tests. |
| Extracted Intake taxonomy boundary | Moved goal/format/tone/asset/guardrail/data vocabularies, alias mapping, allowed-label cleanup, rule-based classification, and missing/question fallback text into `nori.agents.user_profiling.intaker.taxonomy`, with focused taxonomy tests. |
| Extracted Intake image tagger boundary | Moved per-image vision prompt construction, parallel dispatch, failure isolation, and tag filtering into `nori.agents.user_profiling.intaker.image_tagger`, with focused module tests. |
| Consolidated NoteMaker package contract | Moved skill selection, asset curation, note composition, prompt construction, selected-index normalization, and note-field cleanup into class-owned `nori.agents.content_generation.note_maker.package`, with focused package tests. |
| Consolidated CoverDirector package contract | Moved tagged-asset reference selection, legacy draft/reference collection, reference input conversion, and cover prompt construction into class-owned `nori.agents.content_generation.cover_director.package`, with focused package tests. |
| Extracted CoverDirector output boundary | Moved data-uri/http image persistence and output error translation into `nori.agents.content_generation.cover_director.output`, with focused module tests. |
| Consolidated AccountPlanner package contract | Moved `AccountPlannerInput` restoration, image/link evidence merging, asset prompt context, and JSON-only account prompt construction into class-owned `nori.agents.user_profiling.account_planner.package`, with focused package tests. |
| Extracted AccountPlanner fallback boundary | Moved deterministic no-inference fallback result construction into `nori.agents.user_profiling.account_planner.fallback`, with focused fallback tests. |
| Extracted AccountPlanner search boundary | Moved search provider protocol/fallback, keyword cleanup/dedupe, platform id normalization, provider-error isolation, and search row defaulting into `nori.agents.user_profiling.account_planner.search`, with focused search tests. |
| Extracted AccountPlanner result-normalizer boundary | Moved AccountPlanner output cleanup, keyword normalization, search-only merge behavior, and IP portrait benchmark derivation into `nori.agents.user_profiling.account_planner.normalizer`, with focused module tests. |
| Extracted AccountPlanner portrait boundary | Moved benchmark-account cleanup, IP portrait account names/keywords/pillars/creators/cover formats, and search-only benchmark creator refresh into `nori.agents.user_profiling.account_planner.portrait`, with focused portrait tests. |
| Extracted AccountPlanner keyword normalizer boundary | Moved platform-token stripping, keyword-level normalization, reason fallback, search-keyword fallback, and dedupe into `nori.agents.user_profiling.account_planner.keywords`, with focused keyword tests. |
| Centralized fallback error formatter | Added `nori.shared.attach_llm_error`, removed duplicate per-agent `llm_error` formatter helpers, routed analyzer fallback through the same helper, and made caller stage authoritative. |
| Refined optional JSON fallback reasons | `try_stage_json` now preserves empty model responses versus malformed JSON using local shared-helper logic. |
| Added ops planner fallback observability | Added `try_stage_json` and routed Operation/KPI/Calendar optional LLM stages through it so fallback artifacts retain redacted `llm_error` metadata. |
| Added front-pipeline fallback metadata | Added optional metadata on `IntakeResult` and `AccountPlanResult`, and routed Intake/AccountPlanner optional JSON stages through `try_stage_json`. |
| Added analyzer fallback observability | Routed `XHSNoteAnalyzer.analyze_note` optional LLM enhancement through `try_stage_json` and replaced raw exception strings with structured `validation.llm_error`. |
| Routed analyzer session JSON stages through shared helper | `collect_for_session` keyword generation and note labeling now use `call_stage_json(json_mode=True)` and fail fast through `XHSNoteAnalyzerLLMError` / session validation checks. |
| Shared model coercion helpers | Added shared model coercion helpers and routed ops/skill model `from_dict()` normalization through them to avoid drift between duplicated private helpers. |
| Added generation artifact restoration | Added `from_dict()` round trips for `UserAsset`, `AssetBundle`, `CandidateTitle`, `NoteDraft`, and `CoverResult` so package prompt snapshots can be restored without ad hoc parsing. |
| Added front-pipeline restoration | Added `from_dict()` round trips for `UserInput`, `IntakeResult`, `AccountPlannerInput`, and `AccountPlanResult` so intake and account-planning snapshots can be restored before ops handoff. |
| Canonicalized asset dict normalization | Routed NoteMaker and ContentProducer dict assets through `UserAsset.from_dict()` instead of local reconstruction helpers. |
| Added XHS evidence restoration | Added `from_dict()` round trips for `XHSNoteSample` and `XHSSeedSkillDraft` so collected evidence and seed drafts can be restored without ad hoc parsing. |
| Added boolean model coercion | Added shared `bool_value` and routed persisted model booleans through it so string values like `"false"` restore correctly. |
| Hardened config model coercion | `NoriConfig` now coerces model scalar/list/dict fields from YAML before they reach LLM capability guards and kwargs merging. |
| Added explicit config structure errors | Non-mapping YAML top level, `providers`, `models`, and individual provider/model entries now raise `NoriConfigError`, and active usage lookup trims the queried usage key before resolution. |
| Hardened duration option coercion | Added shared `int_list` coercion so scalar config values such as `duration_options: "10"` become `[10]`. |
| Normalized active model selection | `NoriConfig` now normalizes selected `active_models` blocks and reports malformed shapes, blank active keys, invalid scalar keys, and missing `NORI_MODE` blocks through explicit config errors. |
| Aligned runtime mode normalization | `nori.core.llms.current_mode`, `set_mode`, and `ensure_ready` now trim mode values so environment whitespace cannot make readiness checks diverge from `NoriConfig` active model selection. |
| Rejected half-empty model keys | `parse_model_key` now requires non-blank provider and model ids after trimming, so malformed keys such as `openai::` and `::model` fail at the config boundary. |
| Canonicalized model key lookup | `NoriConfig` now stores and resolves model keys as canonical `provider::model`, preventing segment whitespace from dropping configured model capability fields. |
| Required declared model configs | `NoriConfig.resolve` now raises `KeyError: 未配置模型` for provider-backed but undeclared model keys instead of constructing a default `ResolvedModel`. |
| Canonicalized provider and mode keys | Provider ids, `mode`, `NORI_MODE`, and mode block keys are now trimmed before lookup, and blank provider ids fail fast. |
| Canonicalized API key env names | `api_key_env` and `${ENV_VAR}` placeholders now trim environment variable names before lookup and store provider `api_key_env` canonically. |
| Validated OpenAI-compatible client config | Internal `nori.core.llms.client.get_client` and `get_async_client` now raise `LLMClientConfigError` before SDK construction when active provider `api_key` or `base_url` is blank. |
| Shared readiness/client config validation | `nori.core.llms.ensure_ready` now reuses `validate_client_config`, so readiness checks and actual OpenAI-compatible client construction agree on blank `api_key` / `base_url` failures. |
| Validated Google image API key before SDK dispatch | Google native image generation now reuses shared `validate_api_key`, so blank API keys fail with `LLMClientConfigError` and redacted telemetry before `google-genai` construction. |
| Eliminated image active-model double resolution | `nori.core.llms.image` now constructs OpenAI-compatible image clients from the already-resolved model, keeping capability checks and provider dispatch bound to the same model. |
